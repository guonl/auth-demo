server:
  port: 10001

spring:
  application:
    name: uaa-service

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost/security_test?useSSL=false&serverTimezone=UTC
    username: root
    password: 123456
  thymeleaf:
    mode: HTML
    encoding: utf-8
    cache: false

mybatis:
  typeAliasesPackage: com.guonl.model
  mapperLocations: classpath*:mapper/*Mapper.xml


