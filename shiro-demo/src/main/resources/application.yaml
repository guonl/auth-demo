server:
  port: 8081

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost/shiro_test?useSSL=false&serverTimezone=UTC
    username: auth
    password: 123456
  thymeleaf:
    mode: HTML
    encoding: utf-8
    cache: false

mybatis:
  typeAliasesPackage: com.guonl.model
  mapperLocations: classpath*:mapper/*Mapper.xml